# payment-service — Postgres hotel_payments (Cloud SQL private IP)
server:
  port: ${SERVER_PORT:8083}

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://10.149.209.3:5432/hotel_payments}
    username: ${DB_USERNAME:postgres}
    password: "${DB_PASSWORD:HotelPg#2026!}"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
